# Camera Permission And Selection

The scanner verifies secure context and camera API availability before requesting video.

Permission denial, unavailable cameras, and camera track interruption enter viewer-safe fallback rather than leaving the user on an endless scanner screen.

