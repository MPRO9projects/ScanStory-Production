# Runtime Mode Selection

Runtime modes:

- Full: strongest devices, higher frame width, faster detection interval, more tracking points.
- Standard: default mode for capable browsers.
- Lightweight: lower frame width, slower detection interval, fewer tracking points.
- Fallback: direct video playback when camera or scanner runtime is unavailable.

Manual `scanner_mode` override is accepted for rehearsal and debugging.

