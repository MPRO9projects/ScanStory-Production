# Fallback Experience

Fallback is an explicit runtime mode and explicit terminal recovery path.

Safe fallback reasons include secure-context failure, camera permission denial, unavailable camera, OpenCV load failure, scanner timeout, detection timeout, invalid recognition response, target-loss timeout, unsupported device, paused/archived experience, and missing published media.

