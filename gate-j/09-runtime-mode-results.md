# Runtime Mode Results

Automated validation confirmed:

- Full mode has the highest frame width and fastest detection interval.
- Standard mode applies reduced parameters.
- Lightweight mode applies lower resolution and reduced request frequency.
- Fallback mode has zero scanner workload.
- Browser override probes resolved all four modes.

Real-device mode selection remains blocked for Android and iPhone.

