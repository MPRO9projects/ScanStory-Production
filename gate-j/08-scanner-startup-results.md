# Scanner Startup Results

Executed startup checks:

- Browser runtime loaded in Chrome and Edge.
- State path reached `loading_shell`, `checking_capabilities`, and `requesting_camera`.
- Runtime selected full mode on the workstation capability profile.
- Automated state-machine tests passed timeout, target-loss, recovery, and duplicate initialization protections.

Physical camera-ready, OpenCV-ready, first recognition, and first video frame timings were not measured on real devices.

