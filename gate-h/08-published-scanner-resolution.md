# Published Scanner Resolution

`resolve_published_experience()` resolves:

public key -> Experience -> current Published Version -> immutable Trigger snapshots.

It returns a controlled compatibility shell and never reads Draft Trigger state for viewer output.
