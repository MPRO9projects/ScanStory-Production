# Versioning Model

Gate H extends `ExperienceVersion` additively with publication metadata, checksum, idempotency key, immutable marker, rollback source, processing snapshot, notes, and public destination.

Canonical statuses are centralized in `EXPERIENCE_VERSION_STATUSES`.
