# Draft Version Workflow

`ensure_draft_version()` creates or returns one editable Draft Version. If a Published Version exists, its snapshots are copied as a starting point.

Current mutable Trigger edits do not affect the live Published Version until publication.
