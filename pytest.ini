[pytest]
testpaths = tests
markers =
    slow: slower baseline or benchmark-style tests
    cv: computer-vision tests
    security: security baseline tests
    contract: API and compatibility contract tests
addopts = -ra
