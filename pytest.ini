[pytest]
testpaths = tests
markers =
    slow: slower baseline or benchmark-style tests
    cv: computer-vision tests
    security: security baseline tests
    contract: API and compatibility contract tests
    scanner_robustness: synthetic scanner accept/reject robustness pack (see tests/gate_jr/test_scanner_robustness.py)
addopts = -ra
xfail_strict = true
