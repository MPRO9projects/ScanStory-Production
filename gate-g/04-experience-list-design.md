# Experience List Design

The list shows name, status, trigger counts, ready, processing, needs attention, excluded, creator, updated time, and QR foundation status when enabled.

Search, status filter, sorting, and pagination are supported. The list uses aggregate counts and does not read media or processing histories.
