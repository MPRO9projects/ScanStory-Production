# Accessibility And Mobile

Templates include labels, role status text, alert text, keyboard-accessible links/buttons, progress labels, and responsive grids.

Primary workflows are card-based for small screens instead of wide tables.
