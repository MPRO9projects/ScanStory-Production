# Plan Limit Compatibility

Workspace billing is not activated.

Experience creation requires the existing user subscription/trial to be active, but does not increment `projects_used` and does not create billing or Razorpay records. Legacy Project limits remain unchanged.
