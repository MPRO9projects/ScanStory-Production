# Legacy Compatibility

Legacy `/projects`, `/create-project`, `/upload`, Project QR, and `/scanner/<project_id>` routes were not modified except app-level Blueprint registration.

Contracts, security, fast, and full regression suites passed.
