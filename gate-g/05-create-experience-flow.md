# Create Experience Flow

Creators enter name and optional description. Name is required and limited to 255 characters.

On first use, an active personal Workspace is created for the user only inside the enabled workflow. The Experience stays `draft`, has a public key, and has no published version.
