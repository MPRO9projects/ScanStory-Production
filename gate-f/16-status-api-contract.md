# Status API Contract

Gate F provides internal services:

```python
get_trigger_processing_status(trigger_id)
get_experience_processing_status(experience_id)
```

Optional `user_id` checks Workspace membership.

No public endpoint is added in Gate F.
