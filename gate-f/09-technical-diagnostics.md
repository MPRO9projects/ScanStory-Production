# Technical Diagnostics

Technical diagnostics are stored on jobs and events, separate from creator messages.

Diagnostics may include job type, attempt count, worker ID, safe storage key, algorithm version, pipeline version, failed dependency, timestamps, and diagnostic code.

Creator status returns a safe diagnostic ID when a failure exists.
