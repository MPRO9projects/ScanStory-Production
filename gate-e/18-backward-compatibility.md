# Backward Compatibility

Gate E does not modify scanner routes, QR routes, upload behavior, or payment behavior.

`LegacyStorageCompatibility` provides safe read-only resolution for current user/admin media roots and preserves root separation.

Existing Project and ProjectPair media paths remain the active scanner paths.
